MailMessage mail = new MailMessage();
            mail.From = new System.Net.Mail.MailAddress("......@gm.uit.edu.vn");
            SmtpClient smtp = new SmtpClient();
            smtp.Port = 587;
            smtp.EnableSsl = true;
            smtp.DeliveryMethod = SmtpDeliveryMethod.Network;
            smtp.UseDefaultCredentials = false;
            smtp.Credentials = new NetworkCredential(mail.From.Address, "gvlueaxnxdwmwbrw");
            smtp.Host = "smtp.gmail.com";

            //recipient
            mail.To.Add(new MailAddress(txt_Mail.Text));
            mail.IsBodyHtml = true;
            mail.Subject = txt_Subject.Text;
            mail.Body = txt_Content.Text;

            //for (int i = 0; i < attachmentFilename.Length; i++)
            //    mail.Attachments.Add(new Attachment(attachmentFilename[i]));

            smtp.Send(mail);
            MessageBox.Show("Email sent successfully!", "Information", MessageBoxButtons.OK, MessageBoxIcon.Information);